function e(e){return e.toLowerCase().replace(/[^a-z0-9]+/g,`_`).replace(/^_+|_+$/g,``)}var t=/^[a-z][a-z0-9_-]{0,63}$/;function n(n,r={}){let i=r.contextLabel??`context`,a=[],o=n.slot.trim();o||a.push(`slot required`);let s=n.values.filter(e=>e.trim());s.length<2&&a.push(`≥ 2 values`);let c=new Set;for(let n of s){let r=e(n);t.test(r)?c.has(r)?a.push(`value "${n}" collides with another value's label`):c.add(r):a.push(`value "${n}" is not a valid node label`)}return n.contexts.length===0&&a.push(`at least one ${i} required`),n.contexts.forEach((e,t)=>{let n=`${i} ${t+1}`;e.turns.length===0?a.push(`${n}: needs a history turn`):(e.turns.some(e=>!e.content.trim())&&a.push(`${n}: every history turn needs text`),e.turns[e.turns.length-1].role!==`user`&&a.push(`${n}: last turn must be user`),o&&e.turns.some(e=>e.content.includes(o))&&a.push(`${n}: slot must not appear in a history turn`)),e.assistant.trim()?o&&e.assistant.split(o).length-1!=1&&a.push(`${n}: slot must appear once in the assistant turn`):a.push(`${n}: assistant turn required`)}),a}export{n as t};